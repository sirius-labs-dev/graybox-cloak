# build artifacts
target
**/*.rs.bk
node_modules
**/node_modules
**/.next
dist
build
.anchor
.local
.deploy-staging
test-ledger

# editor / OS
.DS_Store
.idea
.vscode
*.swp
.yarn

# tooling caches
.surfpool
.turbo
.next
.vercel

# logs
*.log

# secrets — never commit
.env
.env.local
.env.*.local
*.pem
*.key
id_ed25519*
id_rsa*
secrets/
server-info.md

# runtime config (keypairs and live slice files contain private material)
config/*-keypair.json
config/*-wallet.json
config/demo-wallet.json
config/authority.json
config/oracles/
config/customer.json
config/treasury-slice.json
config/slices.json

# program keypair: regenerate per deployer with `anchor keys sync`
target/deploy/*-keypair.json

# operator-only docs (deployment specifics, server IPs, runbook secrets)
docs/DEPLOY.md
docs/RUNBOOK.md
docs/VERCEL.md
docs/SERVER*.md
docs/private/

# vercel.json removed in favor of next.config.ts rewrites + GPAY_GATEWAY_URL
# env var, so the gateway URL never lands in the repo. Keep this here in
# case someone reintroduces it.
apps/dashboard/vercel.json

# operator local env (sourced manually, e.g. `source ~/.config/g-pay/env`)
.envrc
.env.deploy
